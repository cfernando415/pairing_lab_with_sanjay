<h1><%= @book.title %> (<%= @book.pages%> pages)</h1>
<h2>Authors:</h2>
<ul>
  <% @book.authors.each do |author| %>
  <li><%= link_to author.name, author %>
    <%= button_to "Remove #{author.name}", author.book_authors, method: :delete %>
  </li>
  <% end %>
</ul>
